import { AadharCardDirective } from './aadhar-card.directive';

describe('AadharCardDirective', () => {
  it('should create an instance', () => {
    const directive = new AadharCardDirective();
    expect(directive).toBeTruthy();
  });
});
