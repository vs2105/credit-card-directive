import { AdvanceCssDirective } from './advance-css.directive';

describe('AdvanceCssDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvanceCssDirective();
    expect(directive).toBeTruthy();
  });
});
